/*
	Para gerar apk de produção acrescentar no arquivo node_modules/react-native/react.gradle 
	após o bloco doFirst

	doLast {
	    def moveFunc = { resSuffix ->
		File originalDir = file("${resourcesDir}/drawable-${resSuffix}")
		if (originalDir.exists()) {
		    File destDir = file("${resourcesDir}/drawable-${resSuffix}-v4")
		    ant.move(file: originalDir, tofile: destDir)
		}
	    }
	    moveFunc.curry("ldpi").call()
	    moveFunc.curry("mdpi").call()
	    moveFunc.curry("hdpi").call()
	    moveFunc.curry("xhdpi").call()
	    moveFunc.curry("xxhdpi").call()
	    moveFunc.curry("xxxhdpi").call()
	}

*/

{ "login_type": 0, "email": "weslley.yell@gmail.com", "password": "123456" }
{ "login_type": 1, "facebook_id": "1262188310506669" }
{ "login_type": 2, "celular": "86995676616" }
